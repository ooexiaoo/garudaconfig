/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    distDir: 'public1',
    images: {
        domains: ['images.unsplash.com']
    }
};

export default nextConfig;
